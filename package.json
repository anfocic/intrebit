{
  "name": "intrebit",
  "type": "module",
  "version": "1.0.1",
  "scripts": {
    "dev": "astro dev",
    "og": "node scripts/build-og.mjs",
    "build": "node scripts/build-og.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "hash-password": "node scripts/hash-password.mjs",
    "deploy": "set -a && [ -f .env.local ] && . .env.local; set +a && npm run build && : \"${CLOUDFLARE_ACCOUNT_ID:?Set CLOUDFLARE_ACCOUNT_ID}\" && wrangler pages deploy dist --project-name intrebit --branch main --commit-dirty=true"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.4"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
