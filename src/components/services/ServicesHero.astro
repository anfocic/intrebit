<section class="services-hero">
    <div class="container services-hero__inner">
        <header class="services-hero__header hero-reveal">
            <h1 class="title reveal-1">Build it right. Keep it running.</h1>

            <p class="subtitle reveal-2">
                We help teams ship dependable software and remove the technical debt and operational mess that slows them down.
            </p>

            <div class="actions reveal-3">
                <a class="btn btn--primary" href="/#contact">Get in touch</a>
            </div>
        </header>
    </div>
</section>

<style>
    .services-hero {
        position: relative;
        padding: 17rem 0;
        text-align: center;
        overflow: hidden;
    }


    .container {
        max-width: var(--container-width, 1200px);
        margin: 0 auto;
        padding: 0 2rem;
    }

    .services-hero__inner {
        display: grid;
        place-items: center;
    }

    .services-hero__header {
        max-width: 760px;
    }

    .title {
        font-size: clamp(2.1rem, 4vw, 3.2rem);
        margin: 0 0 1rem;
        letter-spacing: -0.03em;
        line-height: 1.1;
        color: var(--text);
    }

    .subtitle {
        max-width: 650px;
        margin: 0 auto 2.25rem;
        font-size: 1.15rem;
        line-height: 1.65;
        color: var(--text-muted);
    }

    /* --- Buttons & Actions --- */
    .actions {
        display: flex;
        justify-content: center;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .btn {
        transition: transform 0.2s ease, box-shadow 0.2s ease,
        background-color 0.2s;
    }

    .btn--primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
    }

    /* =========================================
       HERO REVEAL — fade up + blur in + stagger
       ========================================= */

    .hero-reveal > * {
        opacity: 0;
        transform: translateY(14px);
        filter: blur(12px);
        animation: heroReveal 700ms cubic-bezier(.2, .8, .2, 1) forwards;
        will-change: opacity, transform, filter;
    }

    /* stagger order */
    .hero-reveal .reveal-1 { animation-delay: 80ms; }
    .hero-reveal .reveal-2 { animation-delay: 200ms; }
    .hero-reveal .reveal-3 { animation-delay: 340ms; }

    @keyframes heroReveal {
        from {
            opacity: 0;
            transform: translateY(14px);
            filter: blur(12px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
            filter: blur(0);
        }
    }

    /* Reduced motion support */
    @media (prefers-reduced-motion: reduce) {
        .hero-reveal > * {
            animation: none !important;
            opacity: 1 !important;
            transform: none !important;
            filter: none !important;
        }
    }

    /* --- Responsive --- */
    @media (max-width: 520px) {
        .services-hero {
            padding: 6rem 0 4rem;
        }

        .subtitle {
            font-size: 1.05rem;
        }
    }
</style>