---
const plausibleDomain = import.meta.env.PUBLIC_PLAUSIBLE_DOMAIN;
---

{
    plausibleDomain && (
        <>
            <script is:inline>
                window.plausible =
                    window.plausible ||
                    function () {
                        (window.plausible.q = window.plausible.q || []).push(arguments);
                    };
            </script>
            <script
                defer
                data-domain={plausibleDomain}
                data-api="https://plausible.fole.dev/api/event"
                src="/js/pl.js"
            />
        </>
    )
}
