---
interface Props {
    path: string;
    label: string;
    className?: string;
}

const { label, path, className } = Astro.props;
---

<li class="nav__item">
    <a href={path} class={className}>{label}</a>
</li>