<div class="section-divider"></div>

<style>
    .section-divider {
        width: 80px;
        height: 1px;
        margin: 0 auto;
        background: color-mix(in srgb, var(--border) 90%, transparent);
        border-radius: 999px;
        opacity: 0.9;
        background: var(--brand-navy); /* dark default */
    }
</style>