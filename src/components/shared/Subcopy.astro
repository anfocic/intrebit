<p class="subcopy">
    <slot />
</p>

<style>
    .subcopy {
        margin: 0;                 /* wrapper gap controls it */
        max-width: 34ch;           /* stops it feeling like a big block */
        color: var(--text-muted);
        opacity: 0.85;
        line-height: 1.6;
        font-size: 1.05rem;
    }
</style>