<div class="contact-intro">
    <h2 class="contact-intro__title">Let's talk.</h2>

    <div class="contact-intro__about">
        <section class="contact-intro__block" aria-label="Contact">
            <h3 class="contact-intro__about-label">CONTACT</h3>
            <p>
                Just tell us what’s broken (or what you want built).<br />
                We’ll reply with a straight answer.
            </p>
        </section>

        <section class="contact-intro__block" aria-label="What to say">
            <h3 class="contact-intro__about-label">WHAT TO SAY</h3>
            <p>
                If you’ve got a deadline, say it.<br />
                If you’ve got a budget range, even better.
            </p>
        </section>

        <section class="contact-intro__block" aria-label="What happens next">
            <h3 class="contact-intro__about-label">WHAT HAPPENS NEXT</h3>
            <p>
                We’ll ask a couple of sharp questions, then propose a plan.<br />
                If we’re not a fit, we’ll tell you fast.
            </p>
        </section>
    </div>
</div>

<style>
    .contact-intro {
        position: sticky;
        top: calc(var(--header-height, 72px) + 2rem);
    }

    .contact-intro__title {
        font-family: var(--font-brand, sans-serif), serif;
        font-size: clamp(2rem, 4vw, 2.75rem);
        font-weight: 750;
        letter-spacing: -0.03em;
        line-height: 1.15;
        margin: 0 0 1.25rem;
        padding-bottom: 2rem;
        color: var(--text);
    }

    /* Turn it into 3 clear blocks */
    .contact-intro__about {
        display: flex;
        flex-direction: column;
        gap: 1.35rem; /* spacing BETWEEN blocks */
        margin-bottom: 2.5rem;
        padding-bottom: 4.5rem;
    }

    .contact-intro__block {
        display: flex;
        flex-direction: column;
        gap: 0.5rem; /* spacing BETWEEN label and text (closer) */
    }

    .contact-intro__about-label {
        margin: 0;
        font-size: 1rem;
        letter-spacing: 0.1em;
    }

    .contact-intro__block p {
        margin: 0;
        font-size: 1rem;
        line-height: 1.6;
    }

    /* Slight breathing room between sentences */
    .contact-intro__block p br {
        content: "";
        display: block;
        margin-top: 0.35rem;
    }

    /* Mobile */
    @media (max-width: 900px) {
        .contact-intro {
            position: static;
        }
    }

    @media (max-width: 520px) {
        .contact-intro__title {
            font-size: 1.75rem;
        }
    }
</style>