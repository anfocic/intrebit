---
import SocialIcon from "../shared/SocialIcon.astro";
import ThemeToggle from "../shared/ThemeToggle.astro";

const year = new Date().getFullYear();
---

<footer class="footer">
    <p class="footer__text">
        © {year} Intrebit. Software built properly.
    </p>

    <ThemeToggle />

    <div class="footer__social">
        <SocialIcon
                icon="mastodon"
                href="https://mastodon.social/@intrebit"
                title="Mastodon"
                rel="me noopener noreferrer"
                className="footer__social-icon footer__social-icon--mastodon"
        />
        <SocialIcon
                icon="linkedin"
                href="https://www.linkedin.com/company/intrebit"
                title="LinkedIn"
                target="_blank"
                rel="noopener noreferrer"
                className="footer__social-icon footer__social-icon--linkedin"
        />
    </div>
</footer>

<style>
    .footer {
        padding: 2rem;
        border-top: 1px solid var(--border);
        display: grid;
        grid-template-columns: 1fr auto auto;
        align-items: center;
        gap: 1.25rem;
        max-width: var(--container-width, 1100px);
        margin: 0 auto;
    }

    .footer__text {
        margin: 0;
        font-size: 0.92rem;
        color: var(--text-muted);
    }

    .footer__social {
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    /* Mobile: horizontal row but compact */
    @media (max-width: 720px) {
        .footer {
            padding: 1.5rem 1.25rem;
            grid-template-columns: 1fr;
            justify-items: center;
            text-align: center;
            gap: 1rem;
        }

        /* Reorder: socials first, then toggle, then copyright */
        .footer__social {
            order: 1;
        }

        .footer > :global(.theme-toggle) {
            order: 2;
        }

        .footer__text {
            order: 3;
            font-size: 0.85rem;
        }
    }
</style>